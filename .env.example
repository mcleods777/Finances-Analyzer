PLAID_CLIENT_ID=your-plaid-client-id
PLAID_SECRET=your-plaid-sandbox-secret
PLAID_ENV=sandbox

# Anthropic API key for the AI daily briefing (optional).
# Without it the briefing card falls back to templated pattern headlines.
ANTHROPIC_API_KEY=
